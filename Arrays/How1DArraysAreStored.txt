

int mat[]={4,5,3,2,6};
================================================
          0   1   2   3   4
 mat -> | 4 | 5 | 3 | 2 | 6 |

int mat[] -> here mat variable stores the address of the array
so when you print "mat.length" it prints the length of the arrays which in case of above example is 5